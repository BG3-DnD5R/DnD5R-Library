function DnD5R_BreathWeaponAttack()
    local result = Dead() | Item()
    if not result.Result then 
        local st = ~SavingThrow(Ability.Dexterity, (8 + context.Source.ProficiencyBonus +GetModifier(context.Source.Constitution)))
        return ConditionResult(st.Result, {}, {}, st.Chance)
    end
    return result
end

function ExtraWeaponAttack()
    return ConditionResult(true)
end